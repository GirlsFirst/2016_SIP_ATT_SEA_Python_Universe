#this is a file to display text
#contains a text class and functions
import pygame
from pygame.locals import *
pygame.init()

# Define some colors
BLACK = (0, 0, 0)
WHITE = (255, 255, 255)
GREEN = (0, 255, 0)
RED = (255, 0, 0)
BLUE = (0, 0, 255)
##
##class Message():

##    def __init__(self,length,height,text_color,font,text):
##        self.length = length
##        self.height = height
##        self.text_color = text_color
##        self.font = font
##        self.text = text

def displayText(self,length,height,text_color,font,text):
    largeText = pygame.font.Font
        
